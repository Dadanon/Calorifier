import 'package:flutter/material.dart';

Widget saveIcon() {
  return Container(
      width: 32,
      alignment: Alignment.center,
      child: const Icon(
        Icons.save,
        size: 32,
      ));
}
