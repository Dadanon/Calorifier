import 'package:flutter/material.dart';

Widget cancelIcon() {
  return Container(
      width: 32,
      alignment: Alignment.center,
      child: const Icon(
        Icons.cancel,
        size: 32,
      ));
}
