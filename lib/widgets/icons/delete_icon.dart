import 'package:flutter/material.dart';

Widget deleteIcon() {
  return Container(
      width: 32,
      alignment: Alignment.center,
      child: const Icon(
        Icons.delete,
        color: Colors.red,
        size: 32,
      ));
}
