import 'package:flutter/material.dart';

Widget addIcon() {
  return Container(
      width: 32,
      alignment: Alignment.center,
      child: const Icon(
        Icons.edit,
        size: 32,
      ));
}
